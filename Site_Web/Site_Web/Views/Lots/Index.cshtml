@model IEnumerable<Site_Web.App_Data.LOT>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des lots</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LOT_NOM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_E_COUREUR_COU.COU_NOM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LOT_NOM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_E_COUREUR_COU.COU_NOM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LOT_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LOT_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LOT_ID })
        </td>
    </tr>
}

</table>
