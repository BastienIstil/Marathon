@model IEnumerable<Site_Web.App_Data.GENERATION>

@{
    ViewBag.Title = "Index";
}

<h2>Generations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.T_E_CLASSEMENT_CLA.CLA_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_E_COUREUR_COU.COU_PRENOM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_R_COURSE_COR.COR_NOM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.T_E_CLASSEMENT_CLA.CLA_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_E_COUREUR_COU.COU_PRENOM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_R_COURSE_COR.COR_NOM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GEN_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GEN_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GEN_ID })
        </td>
    </tr>
}

</table>
