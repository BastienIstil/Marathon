@model IEnumerable<Site_Web.App_Data.PASTAPARTY>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des pastas party</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PAS_NBMAXPARTICIPANT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PAS_NBMAXPARTICIPANT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PAS_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PAS_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PAS_ID })
        </td>
    </tr>
}

</table>
