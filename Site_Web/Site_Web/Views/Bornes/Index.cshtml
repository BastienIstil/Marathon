@model IEnumerable<Site_Web.App_Data.BORNE>

@{
    ViewBag.Title = "Index";
}

<h2>Liste de bornes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BOR_EMPLACEMENT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.T_R_COURSE_COR.COR_NOM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BOR_EMPLACEMENT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.T_R_COURSE_COR.COR_NOM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BOR_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BOR_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BOR_ID })
        </td>
    </tr>
}

</table>
